{
  "buildCommand": "node -e \"const pkg=require('./package.json');delete pkg.scripts.prepare;require('fs').writeFileSync('package.json',JSON.stringify(pkg,null,2))\" && npm ci && npm run build",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "env": {
    "NEXT_DISABLE_SOURCEMAPS": "1",
    "NEXT_TELEMETRY_DISABLED": "1",
    "NODE_OPTIONS": "--max-old-space-size=4096"
  }
}
